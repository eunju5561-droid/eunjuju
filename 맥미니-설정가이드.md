# 맥미니에서 김비서 사용하기

두 가지 방법이 있습니다. 대부분은 **방법 1**이면 충분합니다.

---

## 방법 1 — 그냥 배포된 사이트 쓰기 (설치 불필요) ✅ 추천

맥미니에서 브라우저만 열면 됩니다.

1. https://eunjuju.vercel.app 접속
2. **"Google로 로그인하고 시작하기"** 클릭
3. `jujumath21@gmail.com` 으로 로그인 → 캘린더 권한 허용
4. 끝. 대시보드·회의록·매출·캘린더 모두 사용 가능

> 로그인 안 하면 대시보드 등 모든 페이지가 차단되고 소개 페이지로 돌아갑니다.
> 한 번 로그인하면 24시간 유지됩니다.

---

## 방법 2 — 맥미니에서 로컬로 직접 실행 / 개발

코드를 수정하거나 데이터를 갱신(`npm run brief` 등)하려면 로컬 실행이 필요합니다.

### 사전 준비
- Node.js 설치 (https://nodejs.org)
- GitHub 접근 권한 (이 저장소: `eunju5561-droid/eunjuju`)
- Vercel CLI 로그인 계정 (`eunju5561-droid`)

### 단계

```bash
# 1. 저장소 클론
git clone https://github.com/eunju5561-droid/eunjuju.git
cd eunjuju

# 2. 패키지 설치
npm install

# 3. 비밀값(.env.local) 받아오기 — Vercel에서 자동으로 가져옴
npm i -g vercel        # vercel CLI 없으면
vercel login           # eunju5561@gmail.com 으로 로그인
vercel link            # eunjuju 프로젝트에 연결
vercel env pull .env.local   # GEMINI/GOOGLE_OAUTH 키들이 자동으로 채워짐

# 4. 로컬 서버 실행
npm run dev
# → http://localhost:3000 접속 → Google 로그인
```

> `vercel env pull` 로 받은 `.env.local` 에는 `GEMINI_API_KEY`, `GOOGLE_OAUTH_CLIENT_ID/SECRET/REFRESH_TOKEN`,
> `SESSION_SECRET`, `OAUTH_REDIRECT_URI` 가 들어있습니다. 이 파일은 절대 GitHub에 올리지 마세요 (이미 .gitignore 처리됨).

---

## 데이터 갱신 (방법 2에서만)

```bash
npm run brief      # AI 아침 브리핑 생성 → brief.json
npm run analyze    # 데이터 분석 → analysis.json
npm run meeting    # 회의록 분석 → meeting-result.json
npm run fetch:calendar   # 구글 캘린더 7일치 → calendar-data.json
```

생성된 JSON들은 개인정보라 GitHub에 올라가지 않습니다.
배포본에 반영하려면:

```bash
vercel --prod      # 현재 폴더 전체를 production에 재배포
```

---

## 배포 재배포

```bash
vercel --prod
```

GitHub `main` 에 push 해도 Vercel이 자동 재배포합니다 (단, 위 데이터 JSON은 git에 없으므로
배포본 데이터를 갱신하려면 `vercel --prod` 를 직접 실행하세요).

---

## 구조 요약

| 부분 | 로컬(`server.mjs`) | 배포(Vercel) |
|------|--------------------|--------------|
| 로그인 게이트 | 세션 파일(`.sessions.json`) | 서명 쿠키 + Edge 미들웨어(`middleware.js`) |
| OAuth | `/auth/google`, `/auth/callback` | `api/auth/*.js` |
| 캘린더 | `/api/calendar` | `api/calendar.js` |
| AI 챗 | `/api/chat` | `api/chat.js` |

둘 다 동일하게: **소개 페이지 공개 → Google 로그인 → 대시보드**, 미로그인 시 전 페이지 차단.
